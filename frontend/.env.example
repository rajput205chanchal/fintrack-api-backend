# API base URL — in dev leave as /api (Vite proxy handles it)
# In production set to your deployed backend: https://api.yourdomain.com/api
VITE_API_BASE_URL=/api

# Vite dev proxy target (local backend URL)
VITE_PROXY_TARGET=http://localhost:3000
